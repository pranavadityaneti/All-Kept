{
  "name": "allkept-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "sync:shared": "node scripts/sync-shared.mjs",
    "test:functions": "bash scripts/test-functions.sh",
    "supabase": "supabase",
    "events": "node scripts/dump-events.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "supabase": "^2.117.0"
  },
  "dependencies": {
    "expo-splash-screen": "~57.0.8"
  },
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-native": "0.86.3"
  }
}
