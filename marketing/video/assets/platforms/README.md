# Platform marks

Copied from `packages/platform-assets/assets` (official marks, provenance and checksums in that
package's `provenance.json`). `x.png` is that package's `x-light.png` — the black glyph, for the
white badge the cards draw it on. Kept here so the ad never depends on a file outside its folder.
