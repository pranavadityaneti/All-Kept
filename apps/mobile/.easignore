# What the build machine never needs: 222 MB went up for build 32, most of it this.
# EAS builds from the monorepo root, so paths are relative to the repository.
.git
.claude
private
internal
docs
marketing
supabase
apps/mobile/ios
apps/mobile/android
apps/mobile/node_modules
node_modules
**/*.log
