# The waitlist form posts to the Supabase edge function `waitlist`. Both values are public
# (the same ones the mobile app ships); they live in env, not in git, by convention.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
